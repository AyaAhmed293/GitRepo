@model List<Department>
<table class="table table-bordered table-hover">
  <tr>
      <th>
          ID
      </th>
      <th>Name</th>
  </tr>
    @foreach (var x in Model)
    {
    <tr>
        <td>@x.Id</td>
        <td>@x.Name</td>
    </tr>    
    }
</table>